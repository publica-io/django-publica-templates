============
Installation
============

At the command line::

    $ easy_install django-publica-templates

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-publica-templates
    $ pip install django-publica-templates
